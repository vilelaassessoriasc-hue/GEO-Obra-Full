import pathlib
import sys
import pytest
from starlette.testclient import TestClient

# Este arquivo está em: /app/geoobra_backend_v3/tests/conftest.py
# parents[0]=tests, [1]=geoobra_backend_v3, [2]=/app (raiz do projeto)
ROOT = pathlib.Path(_file_).resolve().parents[2]
p = str(ROOT)
if p not in sys.path:
    sys.path.insert(0, p)

from geoobra_backend_v3.app.main import app  # noqa: E402

@pytest.fixture
def client() -> TestClient:
    return TestClient(app)
